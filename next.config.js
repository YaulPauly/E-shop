/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["firebasestorage.googleapis.com", "allmobiles.com.pe", "home.ripley.com.pe"]
    }
}

module.exports = nextConfig
